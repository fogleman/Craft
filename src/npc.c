#include "npc.h"
#include "npc.h"

const int blocks[256][6] = {
    // w => (left, right, top, bottom, front, back) tiles
    {230, 228, 245, 213, 229, 231}, // 0 - corgi
};

int is_animal(int w) {
	return 0;
}
